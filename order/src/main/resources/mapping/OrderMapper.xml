<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.himoko.order.dao.OrderDao">

    <select id="getUser" resultType="com.himoko.order.pojo.Order">
        SELECT
        id, order_num, money
        FROM t_order
    </select>

</mapper>
